{
    "name": "@onefend/extension",
    "version": "1.0.0",
    "description": "Onefend browser extension for AI security and governance",
    "type": "module",
    "scripts": {
        "dev": "vite build --watch --mode development",
        "build": "tsc && vite build && mkdir -p dist/icons && cp icons/icon-*.png dist/icons/",
        "build:chrome": "npm run build && npm run package:chrome",
        "build:firefox": "TARGET_BROWSER=firefox npm run build && npm run package:firefox",
        "package:chrome": "cd dist && zip -r ../onefend-chrome.zip .",
        "package:firefox": "cd dist && zip -r ../onefend-firefox.zip .",
        "type-check": "tsc --noEmit"
    },
    "dependencies": {
        "webextension-polyfill": "^0.12.0"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.268",
        "@types/node": "^20.11.5",
        "@types/webextension-polyfill": "^0.10.7",
        "terser": "^5.46.1",
        "typescript": "^5.3.3",
        "vite": "^5.0.11",
        "vite-plugin-web-extension": "^4.1.1"
    }
}
